# Appwrite infrastructure

Export the variables documented in the root README, then run:

```sh
npm install
npm run init
```

The initializer is rerunnable: existing database, table, and columns are kept.
The API key is only used locally to create schema resources and must never be
added to either frontend.
